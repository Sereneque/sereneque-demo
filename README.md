# sereneque-demo

Throwaway deployment test for the Sereneque site pipeline.
Static placeholder in `public/`, auto-deployed to GitHub Pages on every push to `main`.

Not the real site. The production repo is `sereneque-site` (private).
